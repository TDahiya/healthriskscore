# Health Risk Predictor

A Flask-based web application to predict health risk tiers (Gold, Silver, Bronze) based on user activity data.

## Setup

1. **Install Requirements:**
   ```bash
   pip install flask scikit-learn numpy pandas
 
```bash
   python app.py
